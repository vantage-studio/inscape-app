{
  "name": "powerhouse-company-local",
  "version": "1.0.0",
  "description": "Local development server for Powerhouse Company website",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "mkdir -p public && cp -r index.html css js fonts wp_optimized manifest.json 404.html public/ 2>/dev/null || true",
    "vercel-build": "npm run build"
  },
  "dependencies": {
    "http": "^0.0.1-security",
    "fs": "^0.0.1-security",
    "path": "^0.12.7",
    "ws": "^8.16.0"
  }
}
